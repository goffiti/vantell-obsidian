{
  "name": "@vantell/vaultscan-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vault scanner + payload builders — the TypeScript parity twin of the vantell-connect skill. Pure: no DOM, no Node APIs, no network.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/ciphers": "^2.3.0",
    "@noble/curves": "^2.3.0",
    "@noble/ed25519": "^2.3.0",
    "@noble/hashes": "^2.3.0",
    "js-yaml": "^4.3.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.6.2",
    "vitest": "^4.1.10"
  }
}
